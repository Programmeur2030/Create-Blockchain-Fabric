package com.divnectar.createblockchain.sound;
/*
import com.divnectar.createblockchain.CreateBlockchain;
import net.minecraft.core.Registry;
import net.minecraft.core.registries.BuiltInRegistries;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.sounds.SoundEvent;
*/
public class ModSounds {
/*
    public static final SoundEvent COIN_CHACHING = registerSoundEvent("coin_chaching");

    private static SoundEvent registerSoundEvent(String name) {
        ResourceLocation id = new ResourceLocation(CreateBlockchain.MODID, name);
        return Registry.register(BuiltInRegistries.SOUND_EVENT, id, SoundEvent.createVariableRangeEvent(id));
    }

    public static void register() {
        // This method is called to initialize the class and register the sounds.
    } */
}
